# Secrets - NUNCA al repo
.env
backtest/.env
*.env

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
backtest/.venv/
venv/
env/
.eggs/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/

# Node
node_modules/
npm-debug.log*

# Datos raw (se regeneran en cada run)
backtest/data/raw/

# Logs
backtest/data/fetch.log
*.log

# Archivos temporales del editor
*.tmp.*
*.swp
.DS_Store
Thumbs.db

# Conflictos de Dropbox (sync entre máquinas)
*Copia en conflicto*
*(conflicted copy)*

# Cache local de UI (se regeneran)
energy_cache.json
state.json
signals_history.json
state_ux_instrumentation.json

# Archivos temporales de Claude Code
.claude/
docs/data/_cava_price_cache.parquet
docs/data/_baseline_price_cache.json
docs/data/_relative_flow_price_cache.parquet
# 45 pares x ~10-27 anios de historia diaria = ~229k filas, ~160MB — muy por
# encima del limite duro de GitHub (100MB/archivo). Regenerable localmente en
# ~2-3 min via reconstruct_relative_flow_historical.py (--save-cache la
# primera vez, --no-fetch despues). Ver wiki/PLAN_RELATIVE_FLOW_LAB_BACKTEST.md.
docs/data/relative_flow_history_reconstructed.jsonl

# Cache de precios del Relative Flow Family Test — regenerable, ver
# research/relative_flow_family_test_v1/relative_family_falsification_test.py
research/relative_flow_family_test_v1/_price_cache.parquet

# Cache de precios de Capitulation Precursors v1 — regenerable, ver
# research/capitulation_precursors_v1/build_dataset.py --save-cache
research/capitulation_precursors_v1/_price_cache.parquet

# Caches de precio/reconstrucción de Cruce Rojo D (auditoría cartera IA +
# backtest sobre universo completo, 2026-08-30) — regenerables, ~60MB+ y
# 21MB respectivamente, ver README.md de cada carpeta. backtest_trades_by_condition.json
# (resultados finales del backtest) y trade_review_signals.csv/.jsonl (el
# documento con Koncorde/RSI/MACD/Flow Score por ticker y día del audit) SÍ
# se commitean — son el resultado, no la caché.
research/koncorde_cross_backtest_2026-08/ohlcv_universe.json
research/koncorde_cross_backtest_2026-08/ohlcv_universe_cleaned.json
research/koncorde_cross_backtest_2026-08/trend_rsi_universe.json
research/trade_review_2026-08/ohlcv_long_history.json
research/trade_review_2026-08/koncorde_reconstructed_raw.json
research/trade_review_2026-08/koncorde_windowed.jsonl
research/trade_review_2026-08/trades_windows.json
